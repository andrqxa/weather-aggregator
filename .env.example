# HTTP server port
FIBER_PORT=3000

# Time interval for fetching weather data
FETCH_INTERVAL=15m

# API key for external provider (leave empty for now)
WEATHER_API_KEY=

# Comma-separated list of default cities
DEFAULT_CITIES=London, Paris, Warsaw
